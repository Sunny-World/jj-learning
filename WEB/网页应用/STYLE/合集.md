# 合集
```
<img>与<p>的垂直间距（默认)用block取消;

fixed定位居中可以用（left=0，right=0，top=0，bottom=0)

设置width为百分比时，如果父元素设置了padding值或子元素有margin时，则会出现滚动条

|符号要用圆角符号打出来才能垂直居中

ie8下注释不管用

ie8下要用低版本jq

兄弟选择符～支持ie7


文本节点换行产生的空隙解决办法：1:浮动；2:父级添加font-size：0；3:代码不换行；

Css IE hack width:90px\0;不能有空格；

Transform translate 居中可以改为margin以兼容IE8
		
关于absolute,谷歌默认left为0，火狐自适应		

```